{
    "host": "localhost",
    "port": 3030,
    "public": "../public/",
    "paginate": {
        "default": 10,
        "max": 50
    },
    "routerHost": "localhost",
    "routerPort": "3031",
    "routerRegisterService": "applications",
    "mongodb": "mongodb://localhost:27017/rtGameserver",
    "maxChannelConnections-clients": 5,
    "maxChannelConnections-backends": 1,
    "client_required": ["created_timestamp", "sended_timestamp", "client_data", "game"],
    "client_data_required": ["id", "token", "network"],
    "game_required": ["sequenzed", "stable"]
}